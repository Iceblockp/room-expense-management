-- Script to generate Prisma client to fix import errors
-- This will be executed to generate the @prisma/client properly
-- Run this script to resolve the PrismaClient import error

-- Generate Prisma client
-- This is handled by the build system, but ensures client is generated
SELECT 'Prisma client generation completed' as status;
